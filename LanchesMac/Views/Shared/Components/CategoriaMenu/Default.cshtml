@* menu suspenso *@

@*cria uma lista não ordenada (<ul>) com a classe "dropdown-menu", que será estilizada como o conteúdo do menu suspenso. *@
<div class="dropdown">
    <button class="btn btn-dark dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Produtos
    </button>
    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
        @* link direto para a ação "List" do "Lanche" sem especificar uma categoria *@
        <a class="dropdown-item" asp-controller="Lanche" asp-action="List">Todos os Produtos</a>
        <div class="dropdown-divider"></div>
        @*inicia um loop foreachque percorre uma coleção de categorias de produtos representados pelo modelo ( Model).
        O código dentro do loop será executado para cada categoria. *@
        @foreach (var categoria in Model)
        {
            <a class="dropdown-item" asp-controller="Lanche" asp-action="List" asp-route-categoria="@categoria.CategoriaNome">@categoria.CategoriaNome</a>
        }
    </div>
</div>

@* <div class="dropdown">
    <a asp-controller="Lanche" asp-action="List" class="dropdown-toggle" 
       data-toggle="dropdown" style="color:white">Produtos   </a>

    <ul class="dropdown-menu">
        @foreach (var categoria in Model)
        {
            <li>
                <a asp-controller="Lanche" asp-action="List" style="color:black"
                   asp-route-categoria="@categoria.CategoriaNome">@categoria.CategoriaNome</a>
            </li>
        }
        <li class="dropdown-divider"></li>
        <li>
            <a asp-controller="Lanche" asp-action="List" asp-route-categoria="" 
               style="color:black">Todos os Produtos</a>
        </li>
    </ul>
</div> *@



